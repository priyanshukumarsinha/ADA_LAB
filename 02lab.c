#include<stdio.h>
#include<conio.h>
void merge(int[],int,int,int);
void merge sort(int[],int,int);
void merge(int a[25],int low, int m)
{
    int b[25],h,i,j,k;
    h=low;
    i=low;
    j=mid+1;
    while(h<=mid)&&(j<=high))
    {
        if(a[h],a[j])
        {
            
        }
    }
}